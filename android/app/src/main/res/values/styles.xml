<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- // MONA - OS Integration -->
    <style name="Theme.MONA" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@color/mona_surface</item>
        <item name="android:navigationBarColor">@color/mona_background</item>
        <item name="android:windowBackground">@color/mona_background</item>
    </style>

    <!-- // MONA - OS Integration -->
    <style name="Theme.MONA.Transparent" parent="@android:style/Theme.Translucent.NoTitleBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
</resources>
