node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env
.env.local
.env.*.local
.firebase/
firebase-applet-config.json

# Paquetes temporales para coordinar deploys con el dueño del proyecto.
# Se regeneran cuando se necesitan, no son fuente de verdad.
firestore-rules-*.zip
DEPLOY-RULES.md

# Archivo de contexto local para Claude Code; se mantiene en disco para
# alimentar futuras sesiones pero no se versiona.
CLAUDE.md
